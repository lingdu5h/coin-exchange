spring:
  application:
    name: member-service
  cloud:
    nacos:
      server-addr: nacos-server:8848
      config:
        file-extension: yaml
  profiles:
    active: dev
#      去 nacos-server 里面拉取 member-service-dev.yaml
#spring:
#  application:
#    name: finance-service
#  cloud:  # nacos地址
#    nacos:
#      server-addr: nacos-server:8848
#      config:
#        file-extension: yaml
#  profiles:
#    active: dev
# 拉取的配置文件的dataID =  finance-service-dev.yaml

